<% if(user_signed_in? && (current_user.has_role? :admin)) %>
  <%= render partial: 'partials/header/admin' %>
<% elsif(user_signed_in? && (current_user.has_role? :teacher)) %>
  <%= render partial: 'partials/header/teacher' %>
<% elsif user_signed_in? %>
  <%= render partial: 'partials/header/student'%>
<% end %>
